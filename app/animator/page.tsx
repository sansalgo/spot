import { SpotBuilder } from "@/components/animator/spot-builder";

export const metadata = {
  title: "Spot — Pixel Animator",
};

export default function AnimatorPage() {
  return <SpotBuilder />;
}
